#include <stdio.h>

int main (int argc, char *argv[])
{
  // boundary
  // collision
  // interpolation
  // update grud
  // calculate force
  // update particle and species
  printf ("%s\n","simplePIC");
  return 0;
}
